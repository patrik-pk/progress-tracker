import styled from 'styled-components'
import {
    DARK,
} from '../../shared/style/Colors'

export const Container = styled.div`
    flex-grow: 1;
    height: 300px;
    margin-left: 30px;
    background: ${DARK};
`